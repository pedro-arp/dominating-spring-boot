spring:
## Active profiles : --spring.profiles.active=mongo     or:
#  profiles:
#    active:
#      - mongo
  application:
    name: anime-service

server:
  port: 8080
  servlet:
#    context-path: /${spring.application.name}
    context-path: /

logging:
  level:
    root: info
    academy.devdojo: info

database:
  url: localhost:3306
  username: mysql
  password: devdojo

